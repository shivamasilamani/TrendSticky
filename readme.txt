Readme
"https://facebook.com/' + resnse.data[i].from.id +  '"

  
 + date.getTimezoneOffset() * 60000
	
	 + ':' date.getSeconds() + '  ' + date.getTimezoneOffset()
	 



- High resolution image for login page
- Facebook permission box design

keytool -genkey -v -keystore siva-status-search.keystore -alias siva -keyalg RSA -keysize 2048 -validity 10000

keytool -exportcert -alias siva -keystore "C:\Program Files\Java\jre7\bin\siva-status-search.keystore" | openssl sha1 -binary | openssl base64

keytool -exportcert -alias siva -keystore siva-status-search.keystore > c:\openssl\bin\debug.txt



